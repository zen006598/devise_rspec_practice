<nav class="fixed top-0 left-0 z-50 w-full px-3 py-2 bg-white shadow md:flex md-items-center md:justify-between">
  <div class="flex items-center justify-between w-full">
    <span class="text-2xl cursor-pointer">
    <%= link_to 'Class_room', root_path %>
    </span>
  <ul class="flex">
    <div class="flex">
      <div class="flex items-center">
        <% if current_user %>
          <li  class="mr-2 text-lg hover:text-major"><%= current_user.email %></li>
          <li class= "text-lg hover:text-major"><%= link_to '登出', destroy_user_session_path, data: { turbo_method: "delete" } %></li>
        <% else %>
          <li class= "mr-2 text-lg hover:text-lime-600"><%= link_to '註冊', new_user_registration_path %></li>
          <li class= "text-lg hover:text-lime-600"><%= link_to '登入', new_user_session_path %></li>  
        <% end %>
      </div>
    </div>
  </ul>
</nav>